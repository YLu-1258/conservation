-- database: /home/eris29/vscode/conservation/conservation/volumes/users.db

-- Use the ▷ button in the top right corner to run the entire file.

SELECT * FROM "student_info";

UPDATE "user_history" SET _progress = 0 WHERE id IN (2,4,6,8,10);